import React from 'react'

import styles from './CoursesSelect.scss'

const TimeSelect = ({ date, courses, selectedCourse, onChangeCourse }) => {
  return <div className={styles.courseSelect} />
}

export default TimeSelect
